export interface User {
    id: number;
    item: string;
    description: string;
    formulation: string;
    batchNo: string;
}

export interface OfficeInfo {
    label: string;
    code: string;
}
